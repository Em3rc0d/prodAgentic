# MK0 Design Map

MK0 user-experience/design evidence lives mainly in `frontend/app/`, `frontend/components/` where present, stylesheet files under `frontend/app/`, and UI change records under `docs/changes/`.

MK1 intentionally replaces the primary information architecture; MK0 UI remains historical evidence.
