# Approval domain

S7 owns explicit authenticated human approval of one exact `ContentRevision` after S6 has made that revision `REVIEWABLE`.

`ApprovalBundleV2` is immutable evidence. It binds frozen Profile/Plan/Research/Content/Visual/QA digests and exact owned asset hashes. Export, scheduling and publication are downstream authority and are intentionally absent from this module.
