# Copy to .env.docker before running docker compose.
# This file contains LOCAL-ONLY example values. Never reuse them in production.

# Optional for S0→S2 acceptance. Leave blank if you only want Profile/Batch planning.
GEMINI_API_KEY=

APP_DEFAULT_LANGUAGE=es
LANGUAGE_MIN_CONFIDENCE=0.6
LANGUAGE_MIN_MARGIN=0.2

# Stable local bootstrap identity. Do not expose this as client input.
PRODAGENTIC_DEPLOYMENT_KEY=local-docker-installation

# The Compose stack is intentionally scoped to certified MK1 S0→S2.
MK1_ENABLED=true
MK1_PROFILE_V2=true
MK1_BATCH_PLANNING=true

# Local operator auth. Change these before using the stack with any non-test data.
PRODAGENTIC_AUTH_ENABLED=true
PRODAGENTIC_ADMIN_USER=admin
PRODAGENTIC_ADMIN_PASSWORD=local-docker-password-change-me
PRODAGENTIC_SESSION_SECRET=local-docker-session-secret-change-me-0123456789abcdef
PRODAGENTIC_SESSION_TTL_SECONDS=43200

# Compose overrides cookie/CORS/frontend values to their safe localhost settings.
PRODAGENTIC_COOKIE_SECURE=false
PRODAGENTIC_COOKIE_SAMESITE=lax

# LinkedIn is outside S0→S2 local acceptance. These placeholders only make the
# integration configuration structurally available; do not authorize or publish.
LINKEDIN_CLIENT_ID=local-compose-not-authorized
LINKEDIN_CLIENT_SECRET=local-compose-not-authorized
LINKEDIN_REDIRECT_URI=http://127.0.0.1:8000/api/integrations/linkedin/callback
PRODAGENTIC_LINKEDIN_TOKEN_KEY=local-compose-linkedin-token-key-change-me-0123456789
LINKEDIN_API_VERSION=202608
LINKEDIN_STATIC_FALLBACK_ENABLED=false

# External workflow integration remains disabled unless deliberately configured.
N8N_WEBHOOK_URL=
N8N_ALLOW_DIRECT_FALLBACK=false
